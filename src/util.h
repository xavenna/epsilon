#ifndef UTIL_H
#define UTIL_H
#include <random>
#include <ctime>


std::string lowercase(std::string);  //converts a string to lowercase
bool isNum(std::string st);  //determines if a string is a valid number
#endif

